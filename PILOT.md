# パイロット導入ランブック(2週間)

LEVEL UP LAB を少人数(5〜10名)で試験導入し、報酬バランスと定着度を検証するための手順書です。

---

## Day 0 — セットアップ(推進者の作業・30分)

### 1. 起動する

```bash
git clone <repo> /opt/levelup-lab && cd /opt/levelup-lab
./scripts/setup.sh
# .env を編集: ADMIN_PASSWORD を設定(空ならランダム生成され起動ログに表示)
#              HTTPS プロキシ配下なら COOKIE_SECURE=1

# 常駐させる場合(推奨):
sudo cp deploy/levelup-lab.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable --now levelup-lab
sudo journalctl -u levelup-lab -f | grep 管理者   # ランダム生成した場合はここでパスワードを控える
```

小規模な動作確認だけなら `./scripts/run.sh` でフォアグラウンド起動できます(詳細は README.md「本番デプロイ」参照)。

### 2. 管理者の初期設定チェックリスト

- [ ] `admin` でログイン → 「アカウント」からパスワードを自分用に変更
- [ ] 信頼できるメンバーを1人 admin に昇格(**管理者2名体制**。最後の1人は降格できない仕様ですが、不在時対応のため)
- [ ] LIBRARY に**最初のコンテンツを3件以上登録**(ツール・記事など。空のLIBRARYは初日の印象を大きく下げます)
- [ ] ショップの価格・「購入後の案内」文を自社の運用に合わせて調整
- [ ] キックオフのお知らせを配信(テンプレは下記)

### 3. キックオフ告知テンプレ

> 📢 LEVEL UP LAB はじまります!社内ツールを使う・学ぶ・改善するほど EXP とポイントが貯まり、称号と権限が上がる仕組みです。まずは「遊び方」ボタンを見て、クエストを1つ完了してみてください。改善提案は投稿するだけで +100 EXP。今週いちばん活動した人を金曜に表彰します🏆

---

## Day 1-14 — 週次の運用ルーチン(推進者・週15分)

管理パネルを開いて上から順に:

| チェック | 見る場所 | アクション |
|---|---|---|
| 未対応の交換が残っていないか | 統計行の「⚠ 未対応の交換」/ ショップ交換の対応 | **48時間以内に履行**して対応済みに。ここが遅れると信頼を失います |
| 停滞者はいないか | メンバー一覧の ⚠(7日以上活動なし) | Slack等で個別に声かけ。「○○のコンテンツ、あなたの業務に近いですよ」が効く |
| 審査待ち提案 | 画面上部「あなたの承認待ち提案 N件」 | **72時間以内に採用/見送り**。放置は投稿意欲を直接削ります |
| 活性度の推移 | 利用トレンド(直近14日) | アクティブ人数が減少傾向なら、お知らせでミニキャンペーンを打つ |
| 使われないコンテンツ | クエスト管理の「達成 N回」 | 2週間で0回のものは差し替え or 紹介のお知らせを配信 |

オフライン活動(勉強会登壇など)は「調整」ボタンでEXPを手動付与(理由必須・本人に通知されます)。

## Day 14 — 振り返り

1. **CSVをエクスポート**(メンバー一覧・全アクティビティ)して以下を確認:
   - WAU(週次アクティブ)は横ばい以上か
   - 提案の投稿数と採用率(採用率が2割未満なら基準が厳しすぎ、8割超なら緩すぎ)
   - ポイントが貯まる一方で使われていないか(→ ショップの品揃えが刺さっていない)
2. **報酬バランスの調整**(すべて管理パネルから可能):
   - みんなが1週間でシルバー(200 EXP)に届かない → クエストEXPを上げる
   - 初日にゴールド(600)到達者が出る → 提案報酬の上限(現在3件/日)やクエストEXPを下げる
3. 本展開の判断とスケール準備(数百人規模までは現構成のままでOK。それ以上は README の PostgreSQL 移行を検討)

---

## トラブルシューティング

| 症状 | 対処 |
|---|---|
| メンバーがパスワードを忘れた | 管理パネル → メンバー行の「PW再設定」(本人の既存ログインは全て無効化されます) |
| 管理者が全員異動した | サーバー上で `sqlite3 instance/levelup.db "UPDATE users SET role='admin' WHERE name='<名前>'"` |
| データを初期化したい | `sudo systemctl stop levelup-lab` → `instance/levelup.db` を削除 → `sudo systemctl start levelup-lab`(シードデータと管理者が再作成されます) |
| バックアップ | `instance/levelup.db` をコピーするだけ(SQLite単一ファイル) |
| サービスが起動しない | `sudo journalctl -u levelup-lab -n 50` でエラーを確認。venv未セットアップなら `./scripts/setup.sh` を再実行 |
